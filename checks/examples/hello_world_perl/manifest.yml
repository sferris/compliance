id: hello_world_perl
name: Hello World Check using Perl
enabled: true

entrypoint: check.pl

timeout: 120

requires:
  - hello_world_bash

parameters:
  ParameterA: foo

variables:
  VariableA: bar

tags:
  - example
  - hello world
  - perl
